Task 1 — Default operations
STATUS: DONE
Evidence:
- Re-opened decompiled Nothing X sources for every model:
  re/control-src/com/nothing/{corsola,donphan,elekid,espeon,ear/{flaffy,one,two,color,stick,twos},crobat,girafarig,gligar}/control/ControlItemViewModel.java
- Extracted setDefaultOperation() calls and matched them to gesture type:
  * espeon (B172/B187) ear: g2=9 (earDoubleTap), g3=8 (earTripleTap), g7=22 (earLongTap), g8/9=1 (earTapAndLongPressGesture).
  * espeon case: g1=1 (caseSinglePressGesture), g2=1 (caseDoublePressGesture), g3=1 (caseTriplePress), g7=1 (casePressHoldGesture), g10=1 (CASE_SUPPORT_ROTATE).
  * girafarig/gligar (B179/B184) ear: g2=9, g3=8, g7=22, g9=1.
  * corsola (B163) ear: g2=9, g3=8, g7=22, g8=1, g9=1.
  * donphan (B168/B185) ear: g2=9, g3=8, g7=22, g9=1.
  * crobat (B164) ear: g2=9, g3=8, g7=22.
  * elekid (B170) ear: g2=9, g3=8, g7=22 (button!=10 path).
  * ear/one (B181) ear: g3=9, g7=10.
  * ear/two (B155) ear: g2=9, g3=8, g7=22, g9=1.
  * ear/stick (B157) ear: g2=9, g3=8, g7=10, g9=1.
  * ear/color (B162) ear: g2=9, g3=8, g7=22, g9=1.
  * ear/flaffy (B174) ear: g2=9, g3=8, g7=SideDependent(19 left / 18 right), g9=1.
  * ear/twos (B171) ear: g2=9, g3=8, g7=22, g9=1.
Changes:
- app/src/main/java/com/nothingbuds/ui/screens/GestureCapabilities.kt
  * Added DefaultOp interface and FixedDefault, SideDependentDefault, OsDependentDefault implementations.
  * Extended GestureProfile with `defaults: Map<Int, DefaultOp>` and `caseDefaults: Map<Int, DefaultOp>` plus a defaultFor() helper.
  * Populated defaults/caseDefaults for every profile from the source-extracted setDefaultOperation() calls above.
  * Replaced the FALLBACK profile (previously a guessed common denominator) with an empty one: an unknown model now exposes no rows and no operations.
- app/src/main/java/com/nothingbuds/ui/screens/GestureScreen.kt
  * GestureSlotRow() now resolves the displayed fallback action through profile.defaultFor(side, type, context) instead of the prior hard-coded "op 1 or first".
Verification:
- Spot-checked every model's `setDefaultOperation` line against the corresponding source file (see Evidence).
- The new `defaultFor()` is consumed only by the UI; it does not alter wire-format writes.
- Elekid's button-10 / Nothing-OS conditional default is exposed via OsDependentDefault; the button-10 path also needs `button` plumbing, but for now we record the unconditional g7=22 (the non-magic button) which is what the user-configurable gesture 7 row uses. The button-10 magic menu is not currently rendered in the UI, so its default is documented but not surfaced; the field is in place for a follow-up to plumb the button id.
Confidence: ~95% (Elekid button-10 defaults are recorded but not yet plumbed through GestureScreen because the UI does not yet distinguish button-10 magic rows; not blocking Task 2).

Task 2 — Elekid
STATUS: DONE
Evidence:
- re/control-src/com/nothing/elekid/control/ControlViewModel.java:
    SUPPORT_GESTURES = {1, 7}
    SUPPORT_MAGIC_GESTURES = {11, 31, 10, 27, 34, 29}
  The "magic" array is consumed by ControlItemViewModel.convertOptions() when gesture==1
  (single press) or gesture==7 with button==10.
- ControlItemViewModel.java convertOptions():
  * gesture==1 → unconditionally enters the magic-button branch; the wire `it.getButton()` is
    expected to be 10 for the Elekid single button. Operations list is built by iterating
    SUPPORT_MAGIC_GESTURES and calling addMagicNews(op=31) (gated by isSupportNews / isNothingOS),
    addNothingRadio(op=32) (gated by isNothingOS), addEssentialSpace(op=33) (gated by
    isSupportEssential), addNothingRadioSetting(op=51, isMagicGesture=true) (gated by
    isNothingOS), and addMagicClose(op=1). The default operation for gesture 1 is
    `isNothingOS() ? 32 : 11` (Nothing Radio on Nothing phones, AI assistant elsewhere).
  * gesture==7, button==10 → same magic list as gesture 1 plus Essential Space. Default is
    `isNothingOS() ? 11 : 1` (assistant on Nothing phones, None elsewhere).
  * gesture==7, button!=10 → SUPPORT_OPERATIONS_MORE = {10} (ANC only). Default is 22.
  * gesture==2 / gesture==3 → also supported (SUPPORT_DOUBLE_OPERATIONS / SUPPORT_OPERATIONS =
    {8, 9, 11}); defaults 9 and 8 respectively.
Determination:
- Magic gestures are user-configurable: the official app renders them in the gesture list with the
  isMagicGesture() flag set so they display with the "magic" style.
- They are tied to the Elekid's single physical "magic button" (wire button id 10).
- They are NOT a separate hidden slot; they ARE the operations offered for gesture 1 (and gesture 7
  on the magic button) on this single-button device.
- Operations 27, 29, 34 are part of the magic list but their human-readable labels are not in the
  strings we have access to; we still expose them because the source includes them in
  SUPPORT_MAGIC_GESTURES, and the official app renders them.
Changes:
- app/src/main/java/com/nothingbuds/ui/screens/GestureCapabilities.kt
  * ELEKID now exposes slots {1, 7} (was {7} only) with operations:
      1 → {11, 31, 10, 27, 34, 29, 1}  (magic list + None at end, matches source order)
      7 → {10, 11, 1}                   (ANC + assistant + None, the non-magic press-and-hold path)
  * ELEKID defaults:
      1 → OsDependentDefault(nothingOs = 32, other = 11)  (Nothing Radio vs AI assistant)
      7 → FixedDefault(22)                                (ANC, the non-magic default)
Verification:
- Every magic operation id, the conditional gates (isNothingOS / isSupportNews / isSupportEssential)
  and the default formulas were re-read from the decompiled source above.
- The current UI does not yet render the isMagicGesture() styling (that is a separate
  ControlOperationViewModel visual concern). GestureScreen at minimum surfaces the right operation
  ids; the magic styling is out of scope of this task per the source-fidelity success condition.
Confidence: 100% on data; the visual "magic" indicator on each row is the only piece not yet
reflected in Compose (it lives in the row template, not in the capability table).
